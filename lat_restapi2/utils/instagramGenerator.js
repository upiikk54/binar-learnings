class Generator {
    static async generateID() {
        return Math.floor(100 + Math.random() * 900);
    }
    // static async generatePassword(password) {
    //     return `password anda-${password}`;
    // }

    static async token(){
        return 
    }
}

module.exports = Generator;