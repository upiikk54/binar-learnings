const AuthenticationController = require("../AuthenticationController");

describe('authorize', () => {
    it('should testing authorize', () => {
        
    });
});

describe('handleLogin', () => {
    it('should testing handleLogin', () => {
        
    });
});

describe('handleRegister', () => {
    it('should testing handleRegister', () => {
        
    });
});

describe('handleGetUser', () => {
    it('should testing handleGetUser', () => {
        
    });
});

describe('createTokenFromUser', () => {
    it('should testing createTokenFromUser', () => {
        
    });
});

describe('decodeToken', () => {
    it('should testing decodeToken', () => {
        
    });
});

describe('encryptPassword', () => {
    it('should testing encryptPassword', () => {
        
    });
});

describe('verifyPassword', () => {
    it('should testing verifyPassword', () => {
        
    });
});